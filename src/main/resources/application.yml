server:
  port: 8081
  ssl:
    enabled: false

spring:
  application:
    name: Volunteer

  datasource:
    url: jdbc:postgresql://localhost:5500/Volunteer
    username: postgres
    password: "Jinwoo.2912_000"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  properties:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect

  main:
    allow-circular-references: true

